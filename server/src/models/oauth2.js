import _ from 'lodash';
import async from 'async';
import mongobless, {ObjectId} from 'mongobless';

@mongobless({collection: 'oAuth2Clients'})
export class oAuth2Client{ 
    
    
}

@mongobless({collection: 'oAuth2AccessTokens'})
export class oAuth2AccessToken{ 
    
    
}

@mongobless({collection: 'oAuth2RefreshToken'})
export class oAuth2RefreshToken{ 
    
    
}